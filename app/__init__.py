"""Package initialisation for the web application.

This file allows the ``app`` directory to be treated as a Python
package.  It does not contain any code but enables absolute imports
within the package when running via ``python -m app.app``.
"""